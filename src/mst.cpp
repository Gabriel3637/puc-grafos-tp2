#include "graph.hpp"
#include <vector>

class mst
{
  private:
    // só pra tirar o erro
    std::vector<int> a;

  public:
    mst(/* args */);
    ~mst();
};

mst::mst(/* args */) {}

mst::~mst() {}
